import React from 'react';
import { Text, StyleSheet, Platform, View } from 'react-native';

const Clima = () => {
    return (
        <View>
            <Text>Bienvenido a CLima</Text>
        </View>
    );
};

export default Clima;